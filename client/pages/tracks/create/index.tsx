import MainLayout from "../../../layouts/MainLayout";
import css from '@/styles/tracksPage.module.scss';

const Index = () => {
    return (
        <MainLayout>
            <h1 className={css.title}>Загрузка треков</h1>
        </MainLayout>
    );
};

export default Index;